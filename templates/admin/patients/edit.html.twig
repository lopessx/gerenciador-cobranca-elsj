{% extends 'base.html.twig' %}

{% block title %}Editar paciente{% endblock %}

{% block body %}
<div class="admin-page">
    <div class="admin-header">
        <h1>Editar paciente</h1>
        <a href="{{ path('admin_patients') }}" class="theme-toggle">Voltar</a>
    </div>

    <form method="post" class="auth-form">
        <label>Nome<input type="text" name="name" value="{{ patient.name }}" required></label>
        <label>E-mail<input type="email" name="email" value="{{ patient.email }}"></label>
        <label>CPF<input type="text" name="cpf" maxlength="11" value="{{ patient.cpf }}" required></label>
        <label>Empresa
            <select name="company_id" required>
                {% for company in companies %}
                    <option value="{{ company.id }}" {% if patient.company and patient.company.id == company.id %}selected{% endif %}>{{ company.name }}</option>
                {% endfor %}
            </select>
        </label>
        <button type="submit">Atualizar</button>
    </form>
</div>
{% endblock %}
